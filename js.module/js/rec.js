const name = "I'm rectngwe"
function area(w,h){
   return showresult( w*h +" sqft")
}

const showresult = (text) => {
   return "the answer of rectangle is " + text
}

export default {name,area,showresult}